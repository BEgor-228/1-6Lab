<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мои заказы</title>
</head>
<body>
    <h1>Мои заказы</h1>
    <table>
        <thead>
            <tr>
                <th>Марка</th>
                <th>Модель</th>
                <th>Цена</th>
                <th>Цвет</th>
                <th>Имя заказчика</th>
                <th>Телефон заказчика</th>
            </tr>
        </thead>
        <tbody>
            {% for order in orders %}
            <tr>
                <td>{{ order.car_brand }}</td>
                <td>{{ order.car_model }}</td>
                <td>{{ order.car_price }}</td>
                <td>{{ order.car_color }}</td>
                <td>{{ order.client_name }}</td>
                <td>{{ order.client_phone }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="/profile">Назад в личный кабинет</a>
</body>
</html>